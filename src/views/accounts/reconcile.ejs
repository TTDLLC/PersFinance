<section class="page-header row">
  <div>
    <h1><%= reconciliation.account.name %> Reconciliation</h1>
    <p>Snapshot <%= reconciliation.latestSnapshotDate || "none" %> at <%= money(reconciliation.latestSnapshotBalance) %>.</p>
  </div>
  <div class="form-actions">
    <a class="button secondary-button" href="/accounts/<%= reconciliation.account.id %>/statements">Statements</a>
    <a class="button secondary-button" href="/accounts/<%= reconciliation.account.id %>/register">Register</a>
  </div>
</section>

<form
  class="form"
  method="post"
  action="/accounts/<%= reconciliation.account.id %>/reconcile"
  data-reconcile-form
  data-snapshot-balance="<%= reconciliation.latestSnapshotBalance %>"
>
  <div class="grid-form">
    <label>Statement Date
      <input type="date" name="statementDate" value="<%= reconciliation.statementDate %>" required>
    </label>
    <label>Ending Balance
      <input type="number" step="0.01" name="endingBalance" value="<%= reconciliation.endingBalance %>" required data-ending-balance>
    </label>
  </div>

  <div class="metric-grid reconciliation-summary">
    <div class="metric-card">
      <span>Statement Ending Balance</span>
      <strong data-statement-ending><%= money(reconciliation.endingBalance) %></strong>
    </div>
    <div class="metric-card">
      <span>Calculated Reconciled Balance</span>
      <strong data-calculated-balance><%= money(reconciliation.calculatedReconciledBalance) %></strong>
    </div>
    <div class="metric-card">
      <span>Difference</span>
      <strong data-reconcile-difference class="<%= reconciliation.difference === 0 ? "positive" : "negative" %>"><%= accountingMoney(reconciliation.difference) %></strong>
    </div>
  </div>

  <table>
    <thead>
      <tr>
        <th>Selected</th>
        <th>Date</th>
        <th>Description</th>
        <th>Amount</th>
        <th>Status</th>
      </tr>
    </thead>
    <tbody>
      <% if (!reconciliation.eligibleTransactions.length) { %>
        <tr><td class="empty-state" colspan="5">No eligible transactions to reconcile.</td></tr>
      <% } %>
      <% reconciliation.eligibleTransactions.forEach((transaction) => { %>
        <tr>
          <td>
            <label class="checkbox compact-checkbox">
              <input
                type="checkbox"
                name="selectedTransactionIds"
                value="<%= transaction.id %>"
                data-reconcile-transaction
                data-amount="<%= transaction.amount %>"
                <%= reconciliation.selectedTransactionIds.has(transaction.id) ? "checked" : "" %>
              >
            </label>
          </td>
          <td><%= transaction.date %></td>
          <td><%= transaction.description %></td>
          <td class="<%= signedClass(transaction.amount) %>"><%= accountingMoney(transaction.amount) %></td>
          <td><span class="status"><%= label(transaction.status) %></span></td>
        </tr>
      <% }) %>
    </tbody>
  </table>

  <div class="form-actions">
    <button type="submit" data-complete-reconciliation <%= reconciliation.difference === 0 ? "" : "disabled" %>>Complete Reconciliation</button>
  </div>
</form>
