# v1.4.0-internal

Date: 2026-06-18

## Summary

PersFinance v1.4.0-internal marks the current internal-use state after the Step 1 through Step 4 work, dashboard archived-account cleanup, and final pre-version cleanup pass.

## Included Steps and Features

- Step 1: Import and Foundation Review.
- Step 2: Transfers and Future Commitments.
- Step 3: Forecasting.
- Step 4: Scenarios and Forecast Overlay.
- Dashboard archived-account visibility cleanup.
- Final cleanup before versioning:
  - Removed `connect-flash` and replaced it with local flash middleware to resolve the Node `[DEP0044] util.isArray` warning.
  - Added the Step 4 migration duplicate-name preflight before creating the active scenario `lower(name)` unique index.
  - Cleaned merged Step 4 rescue and dashboard cleanup branches.

## Validation

The following commands were run sequentially before marking this version:

```sh
npm run build
npm run test:register
npm run test:forecast
npm run test:step4-scenarios
git diff --check
```

## Known Notes

- This is an internal-use release.
- GitHub status checks are not configured yet.
- Real-world usage testing begins after this version.
- `package.json` uses semver-compatible `1.4.0`; the internal release label is tracked by this document and the `v1.4.0-internal` git tag.

## Parked Future Items

- Future 30/60-day register view after Step 6.
- Multi-user transfer locking.
- Unrelated old merged branch `feature/step-2-transfers-future-commitments` was left alone unless intentionally cleaned later.
